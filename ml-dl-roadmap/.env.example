# Optional — the app runs fully offline with localStorage when these are unset.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Future email notification provider (in-app notifications work without this)
VITE_EMAIL_PROVIDER_API_KEY=
